#include <stdlib.h>
#include <stdio.h>

int main() {
	float a, b;
	float c;
		printf("Print a first symbol:");
		scanf("%f", &a);
		printf("Print a second symbol:");
			scanf("%f", &b);
		c = (a + b) / 2;
		printf("Average number is %.3f\n", c);
		
}
